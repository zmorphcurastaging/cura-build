pipeline {
    agent none
    stages {
        stage ('Run build') {
            agent { label 'jworker' }
            steps {
                bash './docker/linux/build.sh'
            }
        }
    }
}
