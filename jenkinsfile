pipeline {
    agent none
    stages {
        stage ('Run build') {
            agent { label 'jworker' }
            steps {
            sh 'sudo pwd'
                //sh 'sudo ./docker/linux/build.sh'
            }
        }
    }
}
