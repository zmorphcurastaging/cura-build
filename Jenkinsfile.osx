pipeline {
    agent {label 'osx'}

    stages {
        
        stage ('Run build') {
            steps {
                sh 'ls'
                sh 'mkdir build'
                sh 'cd build'
                sh 'cmake ..'
                sh 'make package'
            }
        }

    }
    

}