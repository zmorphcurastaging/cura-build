pipeline {
    agent {label 'WS2019_1809'}

    stages {
        
        stage ('run bash') {            
            steps {
                powershell './docker/windows/build.ps1 -someArg $env:someArg'
                // /powershell './docker/windows/build.ps1 -someArg $env:someArg'
            }
        }
   


    }

}