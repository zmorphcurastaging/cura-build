pipeline {
    agent {label 'WS2019_1809'}

    stages {
        
        stage ('run bash') {            
            steps {
                winpty powershell './docker/windows/build.ps1 -someArg $env:someArg'
                // /powershell './docker/windows/build.ps1 -someArg $env:someArg'
            }
        }
   


    }

}