pipeline {
    agent {label 'WS2019_1809'}

    stages {
        
        stage ('run bash') {            
            steps {
                powershell 'echo "do testu"'
                //bat 'powershell ./docker/windows/build.ps1'
                // /powershell './docker/windows/build.ps1 -someArg $env:someArg'
            }
        }
   


    }

}